datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
  output   = "../source/core/datastore-generated"
}

model ChatChannel {
  id         String @unique
  module     String
}
